services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: valyoued
    ports:
      - "5432:5432"
    volumes:
      - valyoued_pgdata:/var/lib/postgresql/data

volumes:
  valyoued_pgdata:
