# Loaded automatically when running `vite` / `pnpm dev` (development mode).
# Stub auth is enabled automatically when no Clerk publishable key is set (see `vite.config.ts`).
# To force stub auth while a key is present, set `VITE_AUTH_STUB_MODE=1` in `.env.local`.
