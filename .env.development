# Loaded automatically when running `vite` / `pnpm dev` (development mode).
# Override in `.env.local`: use real `VITE_CLERK_PUBLISHABLE_KEY` and `VITE_AUTH_STUB_MODE=0` if needed.
VITE_AUTH_STUB_MODE=1
